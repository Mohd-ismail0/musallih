/**
 * Musallih mobile theme — shared design tokens.
 * Dark-first, teal/emerald accent.
 */
import { colors, fonts, spacing, radius } from "@musallih/shared";

export const theme = {
  colors,
  fonts,
  spacing,
  radius,
} as const;
